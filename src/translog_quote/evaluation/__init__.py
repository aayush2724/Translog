"""Evaluating the extraction pipeline against real client cases.

    client PDF -> deterministic text -> thread split -> CLIENT messages only
        -> existing ExtractionPort -> ExtractionResult
        -> compare against manually reviewed ground truth

Two rules this package exists to enforce:

1. **Only client-authored messages reach the model.** A Translog reply in the
   same thread restates weight, dimensions and commodity; scoring against those
   would measure nothing but our own words coming back.
2. **Ground truth is written by hand, from the PDFs.** Never generated by the
   model it is meant to grade.

Live model calls happen only through this package's CLI, never through pytest.
"""
