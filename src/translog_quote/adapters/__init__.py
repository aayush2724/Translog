"""Adapters (L0) — concrete implementations of ports.

Empty at Phase 1 by design. Creating stubs that pretend to reach OpenRouter or
WebCargo would make the smoke tests pass while proving nothing, so the packages
below carry only a note of what will implement which port, and when.

Nothing outside `bootstrap` may import from here.
"""
