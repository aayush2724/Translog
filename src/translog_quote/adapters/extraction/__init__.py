"""adapters.extraction

Will implement ExtractionPort.

OpenRouterQwenAdapter plus a response cache, so the demo replays a recorded real
response and stays offline and deterministic. Blocked on AMB-2 for the exact
model slug.
"""
